create table lo_m_comment_destination_preset_target (
preset_id varchar(100) not null 
,preset_item_no integer not null 
,recipient_type varchar(100) not null 
,target_type varchar(100) not null 
,matter_attr_cd varchar(100) 
,public_group_set_cd varchar(100) 
,public_group_cd varchar(100) 
,company_cd varchar(100) 
,user_cd varchar(100) 
,sakujo_flg  varchar(20) not null 
,touroku_sha  varchar(100) not null 
,touroku_bi  timestamp(6) without time zone not null 
,koushin_sha  varchar(100) not null 
,koushin_bi  timestamp(6) without time zone not null 
,primary key (preset_id,preset_item_no) 
);
